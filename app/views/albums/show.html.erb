<p id="notice"><%= notice %></p>

<div>
<% @album.images.each do |image, index| %>
<%= image_tag(image.url) %>
<%= link_to "Delete", album_path(@album, index), 
:method => :delete, data: { confirm: "Are you sure you?" } %>

<% end %>
<p>
  <strong>Title:</strong>
  <%= @album.title %>
</p>

<%= link_to 'Edit', edit_album_path(@album) %> |
<%= link_to 'Back', albums_path %>
